<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Quiz | MedQuiz Admin</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800 min-h-screen font-sans">

  <div class="max-w-4xl mx-auto mt-10 bg-white p-8 rounded-xl shadow-md relative">
    <!-- Dashboard Button -->
    <div class="absolute top-4 right-4">
      <a href="/admin/dashboard" class="bg-gray-200 text-gray-700 px-4 py-2 rounded hover:bg-gray-300">
        ← Dashboard
      </a>
    </div>

    <h1 class="text-3xl font-bold text-center text-blue-600 mb-6">Edit Quiz</h1>

    <% if (error) { %>
      <p class="text-red-500 text-center mb-4"><%= error %></p>
    <% } %>
    <% if (success) { %>
      <p class="text-green-500 text-center mb-4"><%= success %></p>
    <% } %>

    <form action="/admin/quiz/<%= quiz._id %>/edit" method="POST" id="quizForm">
      <div class="grid grid-cols-1 gap-4 mb-6">
        <input type="text" name="title" placeholder="Quiz Title" value="<%= quiz.title %>" required class="w-full border border-gray-300 rounded px-4 py-2" />
        <input type="text" name="subject" placeholder="Subject" value="<%= quiz.subject %>" required class="w-full border border-gray-300 rounded px-4 py-2" />
        <input type="text" name="topic" placeholder="Topic" value="<%= quiz.topic %>" required class="w-full border border-gray-300 rounded px-4 py-2" />
        <textarea name="description" placeholder="Description (optional)" class="w-full border border-gray-300 rounded px-4 py-2"><%= quiz.description || '' %></textarea>
      </div>

      <div id="questionsContainer">
        <!-- JS will populate this with quiz.questions -->
      </div>

      <div class="text-right mt-4">
        <button type="button" onclick="addQuestion()" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">+ Add Question</button>
      </div>

      <input type="hidden" name="questions" id="questionsInput" />

      <div class="text-center mt-6">
        <button type="submit" onclick="prepareQuestions()" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition">
          Update Quiz
        </button>
      </div>
    </form>
  </div>

  <!-- Pass existing questions to JS -->
  <script id="oldData" type="application/json">
    <%- JSON.stringify(quiz.questions || []) %>
  </script>
  
  <script src="/scripts/formScript.js"></script>
</body>
</html>
