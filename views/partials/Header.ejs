<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || 'Past Papers Platform' %></title>
  <link rel="stylesheet" href="/stylesheet/output.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />

</head>
<body class="bg-gray-50 font-sans text-gray-800 min-h-screen flex flex-col">

<nav class="bg-white shadow-md">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <div class="flex items-center">
        <i class="fas fa-book-open text-blue-600 text-2xl mr-2"></i>
        <span class="text-xl font-bold text-gray-900">Doctor's Quizz</span>
      </div>

      <!-- Hamburger (mobile only, guests only) -->
      <% if (!admin && !user) { %>
        <div class="md:hidden flex items-center">
          <button 
            id="mobileMenuButton"
            type="button"
            class="inline-flex items-center justify-center p-2 rounded-md text-gray-800 hover:text-gray-900 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500"
            aria-expanded="false"
          >
            <span class="sr-only">Open main menu</span>
            <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>
      <% } %>

      <!-- Desktop Menu -->
      <div class="hidden md:flex space-x-4 items-center">
        <a href="/home" class="px-3 py-2 rounded-md text-sm font-medium 
          <%= currentRoute === '/home' ? 'text-blue-600 underline font-semibold' : 'text-gray-700 hover:bg-gray-100' %>">Home</a>

        <a href="/quiz-section" class="px-3 py-2 rounded-md text-sm font-medium 
          <%= currentRoute === '/quiz-section' ? 'text-blue-600 underline font-semibold' : 'text-gray-700 hover:bg-gray-100' %>">Quiz Section</a>

        <% if (admin) { %>
          <a href="/admin/dashboard" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100">Dashboard</a>
          <form action="/admin/logout" method="POST" class="inline">
            <button class="px-3 py-2 rounded-md text-sm font-medium text-white bg-red-500 hover:bg-red-600">Logout</button>
          </form>
        <% } else if (user) { %>
          <a href="/user/profile" class="px-3 py-2 rounded-md text-sm font-medium text-green-600 hover:underline">Profile</a>
          <form action="/user/logout" method="POST" class="inline">
            <button class="px-3 py-2 rounded-md text-sm font-medium text-white bg-red-500 hover:bg-red-600">Logout</button>
          </form>
        <% } else { %>
          <a href="/features" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100">Features</a>
          <a href="/pricing" class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100">Pricing</a>
          <a href="/user/login" class="px-3 py-2 rounded-md text-sm font-medium text-blue-600 hover:underline">Login</a>
          <a href="/user/signup" class="px-3 py-2 rounded-md text-sm font-medium bg-blue-600 text-white hover:bg-blue-700">Sign Up</a>
        <% } %>
      </div>
    </div>
  </div>

  <!-- Mobile Dropdown (guests only) -->
  <% if (!admin && !user) { %>
    <div id="mobileMenu" class="hidden md:hidden bg-white border-t border-gray-200">
      <div class="px-4 pt-2 pb-4 space-y-1">
        <a href="/home" class="block px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100">Home</a>
        <a href="/quiz-section" class="block px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100">Quiz Section</a>
        <a href="/features" class="block px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100">Features</a>
        <a href="/pricing" class="block px-3 py-2 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-100">Pricing</a>
        <a href="/user/login" class="block px-3 py-2 rounded-md text-sm font-medium text-blue-600 hover:underline">Login</a>
        <a href="/user/signup" class="block px-3 py-2 rounded-md text-sm font-medium bg-blue-600 text-white hover:bg-blue-700">Sign Up</a>
      </div>
    </div>
  <% } %>
</nav>

